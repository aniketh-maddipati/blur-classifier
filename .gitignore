# Virtual environment
.venv/

# Large image datasets — commit only the directory structure, not the files
dataset/
dataset_candidates/

# Result CSVs — generated, not source-controlled
results/*.csv

# Python artifacts
__pycache__/
*.pyc

# Local secrets
.env

# macOS metadata
.DS_Store
venv/
dataset_candidates*/
.pytest_cache/
